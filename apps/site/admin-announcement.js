(()=>{
  if(window.__COSPLAYCHESS_ADMIN_ANNOUNCEMENT__)return;window.__COSPLAYCHESS_ADMIN_ANNOUNCEMENT__=true;
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const client=()=>window.COSPLAYCHESS_DB||window.getCosplayChessDb?.();
  const defaults={enabled:false,theme:'gold',eyebrow:'AVISO DO COSPLAYCHESS',title:'Novidades a caminho',message:'Aguarde: nos próximos dias anunciaremos as próximas novidades do CosplayChess.',imageUrl:'',buttonLabel:'',buttonUrl:'',openNewTab:false,dismissible:true,frequency:'always',startAt:'',endAt:'',accentColor:'#d4aa5c',size:'normal'};
  let doc={...defaults},loaded=false,saving=false,uploading=false;
  const normalize=x=>{x=x&&typeof x==='object'?x:{};return {...defaults,...x,enabled:x.enabled===true,dismissible:x.dismissible!==false,openNewTab:x.openNewTab===true};};
  const icon=t=>t==='maintenance'?'⚙':t==='event'?'♞':t==='info'?'i':'✦';
  function style(){if(document.querySelector('link[data-admin-announcement-style]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='./admin-announcement.css?v=20260824-ann1';l.dataset.adminAnnouncementStyle='1';document.head.appendChild(l);}
  function root(){let r=$('#announcement');if(!r){r=document.createElement('section');r.id='announcement';r.className='v6-view v6-management cc-ann-admin';r.dataset.view='announcement';r.hidden=true;$('.v6-main')?.appendChild(r);}return r;}
  function openAnnouncement(push=true){const r=root();if(!r)return;$$('.v6-view').forEach(v=>{const on=v===r;v.hidden=!on;v.classList.toggle('is-active',on);});$$('.v6-nav a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#announcement'));document.body.dataset.adminView='announcement';if(push&&location.hash!=='#announcement')history.replaceState({},'',`${location.pathname}${location.search}#announcement`);r.scrollTop=0;}
  function nav(){const n=$('.v6-nav');if(!n)return null;let a=n.querySelector('a[href="#announcement"]');if(!a){const rules=$$('.v6-nav a').find(x=>x.textContent.includes('Regras'));const blog=$$('.v6-nav a').find(x=>x.textContent.includes('Blog / Notícias'));a=document.createElement('a');a.href='#announcement';a.innerHTML='<i>◎</i><span>Modal / Aviso</span>';(rules||blog)?.insertAdjacentElement('afterend',a);if(!rules&&!blog)n.appendChild(a);}if(!a.dataset.annRoute){a.dataset.annRoute='1';a.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();openAnnouncement(true);},{capture:true});}return a;}
  const status=(text,type='')=>{const el=$('#announcement .cc-ann-status');if(el){el.textContent=text;el.className=`cc-ann-status ${type}`.trim();}};
  function previewMarkup(){const accent=/^#[0-9a-f]{6}$/i.test(doc.accentColor||'')?doc.accentColor:'#d4aa5c',size=['compact','wide'].includes(doc.size)?doc.size:'';return `<div class="cc-ann-preview ${size}" style="--cc-ann-accent:${accent}">${doc.imageUrl?'<div class="cc-ann-preview-media"></div>':''}<div class="cc-ann-preview-body"><div class="cc-ann-preview-icon">${esc(icon(doc.theme))}</div>${doc.eyebrow?`<span class="cc-ann-preview-eyebrow">${esc(doc.eyebrow)}</span>`:''}<h3>${esc(doc.title||'Aviso')}</h3>${doc.message?`<p>${esc(doc.message)}</p>`:''}${doc.buttonLabel?`<span class="cc-ann-preview-cta">${esc(doc.buttonLabel)}</span>`:''}</div></div>`;}
  function updatePreview(){const stage=$('#announcement .cc-ann-preview-stage');if(!stage)return;stage.innerHTML=previewMarkup();if(doc.imageUrl){const media=$('.cc-ann-preview-media',stage);if(media)media.style.backgroundImage=`url("${String(doc.imageUrl).replace(/["\\]/g,'\\$&')}")`;}const pill=$('#announcement .cc-ann-active-pill');if(pill){pill.classList.toggle('on',doc.enabled);pill.lastChild.textContent=doc.enabled?' Ativo':' Inativo';}}
  function render(){const r=root();if(!r)return;r.className='v6-view v6-management cc-ann-admin';r.dataset.view='announcement';r.innerHTML=`<div class="cc-ann-head"><div><h2>Modal / Aviso do Site</h2><p>Crie avisos de manutenção, comunicados ou anúncios do próximo evento na página inicial.</p></div><div class="cc-ann-actions"><a class="cc-ann-btn" href="./index.html" target="_blank" rel="noopener">Ver home ↗</a><button class="cc-ann-btn gold" type="button" data-ann-save>Salvar e publicar</button></div></div><div class="cc-ann-status">${loaded?'Configuração sincronizada.':'Carregando configuração...'}</div><div class="cc-ann-layout"><article class="cc-ann-panel"><div class="cc-ann-panel-title"><b>Conteúdo e comportamento</b><span id="ccAnnActivePill" class="cc-ann-active-pill ${doc.enabled?'on':''}"><i></i>${doc.enabled?' Ativo':' Inativo'}</span></div><div class="cc-ann-form"><label class="cc-ann-check"><input type="checkbox" data-ann-field="enabled" ${doc.enabled?'checked':''}> Exibir o modal na página inicial</label><label><span>Tipo</span><select data-ann-field="theme"><option value="gold" ${doc.theme==='gold'?'selected':''}>Aviso geral</option><option value="maintenance" ${doc.theme==='maintenance'?'selected':''}>Manutenção</option><option value="event" ${doc.theme==='event'?'selected':''}>Próximo evento</option><option value="info" ${doc.theme==='info'?'selected':''}>Informação</option></select></label><label><span>Tamanho</span><select data-ann-field="size"><option value="compact" ${doc.size==='compact'?'selected':''}>Compacto</option><option value="normal" ${doc.size==='normal'?'selected':''}>Normal</option><option value="wide" ${doc.size==='wide'?'selected':''}>Largo</option></select></label><label class="wide"><span>Texto pequeno acima do título</span><input data-ann-field="eyebrow" value="${esc(doc.eyebrow)}" placeholder="AVISO DO COSPLAYCHESS"></label><label class="wide"><span>Título</span><input data-ann-field="title" value="${esc(doc.title)}" placeholder="Estamos em manutenção"></label><label class="wide"><span>Mensagem</span><textarea data-ann-field="message" placeholder="Escreva o comunicado...">${esc(doc.message)}</textarea></label><label class="wide"><span>Imagem do modal</span><div class="cc-ann-image-tools"><input data-ann-field="imageUrl" value="${esc(doc.imageUrl)}" placeholder="URL da imagem ou envie do computador"><button type="button" data-ann-upload>Enviar imagem</button><button type="button" data-ann-remove-image>Remover</button></div><input id="ccAnnImageFile" type="file" accept="image/jpeg,image/png,image/webp,image/gif" hidden></label><label><span>Texto do botão</span><input data-ann-field="buttonLabel" value="${esc(doc.buttonLabel)}" placeholder="Saiba mais"></label><label><span>Link do botão</span><input data-ann-field="buttonUrl" value="${esc(doc.buttonUrl)}" placeholder="https://..."></label><label class="cc-ann-check"><input type="checkbox" data-ann-field="openNewTab" ${doc.openNewTab?'checked':''}> Abrir botão em nova aba</label><label class="cc-ann-check"><input type="checkbox" data-ann-field="dismissible" ${doc.dismissible?'checked':''}> Permitir que o visitante feche</label><label><span>Frequência</span><select data-ann-field="frequency"><option value="always" ${doc.frequency==='always'?'selected':''}>Sempre que abrir a home</option><option value="session" ${doc.frequency==='session'?'selected':''}>Uma vez por sessão</option><option value="once" ${doc.frequency==='once'?'selected':''}>Uma vez por navegador</option></select></label><label><span>Cor de destaque</span><div class="cc-ann-color-row"><input type="color" data-ann-field="accentColor" value="${/^#[0-9a-f]{6}$/i.test(doc.accentColor||'')?doc.accentColor:'#d4aa5c'}"><input data-ann-field="accentColor" value="${esc(doc.accentColor)}" maxlength="7"></div></label><label><span>Começar em (opcional)</span><input type="datetime-local" data-ann-field="startAt" value="${esc(doc.startAt)}"></label><label><span>Encerrar em (opcional)</span><input type="datetime-local" data-ann-field="endAt" value="${esc(doc.endAt)}"></label></div><div class="cc-ann-help">Dica: para um bloqueio de manutenção, desmarque “Permitir que o visitante feche”. Para um anúncio comum, mantenha essa opção ligada. Ao editar e salvar novamente, os modos “uma vez” voltam a aparecer porque o comunicado recebe uma nova versão.</div></article><aside class="cc-ann-panel cc-ann-preview-wrap"><div class="cc-ann-panel-title"><b>Prévia ao vivo</b><span>HOME</span></div><div class="cc-ann-preview-stage">${previewMarkup()}</div></aside></div>`;updatePreview();}
  async function load(){if(loaded)return;const d=client();if(!d){loaded=true;render();return;}try{const {data,error}=await d.from('cosplay_site_content').select('content').eq('key','site_announcement').maybeSingle();if(error)throw error;doc=normalize(data?.content);loaded=true;render();}catch(e){console.error('[Admin Announcement]',e);doc={...defaults};loaded=true;render();status('Não foi possível carregar do banco. Exibindo configuração padrão.','error');}}
  async function save(){if(saving)return;const d=client();if(!d)return status('Banco indisponível.','error');saving=true;const btn=$('[data-ann-save]');if(btn)btn.disabled=true;status('Salvando...');try{const {data:{user}}=await d.auth.getUser();const payload={...doc,updatedAt:new Date().toISOString()};const now=new Date().toISOString();const {error}=await d.from('cosplay_site_content').upsert({key:'site_announcement',content:payload,published:true,updated_at:now,updated_by:user?.id||null},{onConflict:'key'});if(error)throw error;doc=payload;status(doc.enabled?'Modal salvo e ativo na página inicial.':'Configuração salva. O modal está desativado.','success');}catch(e){console.error('[Admin Announcement Save]',e);status(e.message||'Erro ao salvar o modal.','error');}finally{saving=false;if(btn)btn.disabled=false;}}
  async function upload(file){if(uploading||!file)return;const d=client();if(!d)return status('Banco indisponível.','error');if(!/^image\/(jpeg|png|webp|gif)$/i.test(file.type))return status('Use JPG, PNG, WebP ou GIF.','error');if(file.size>20*1024*1024)return status('A imagem deve ter no máximo 20 MB.','error');uploading=true;$('#announcement')?.classList.add('cc-ann-uploading');status('Enviando imagem...');try{const ext=(file.name.split('.').pop()||'jpg').replace(/[^a-z0-9]/gi,'').toLowerCase();const path=`announcements/${Date.now()}-${Math.random().toString(36).slice(2,8)}.${ext}`;const {error}=await d.storage.from('cosplaychess-site-media').upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(error)throw error;const {data}=d.storage.from('cosplaychess-site-media').getPublicUrl(path);doc.imageUrl=data?.publicUrl||'';render();status('Imagem enviada. Salve para publicar.','success');}catch(e){console.error('[Admin Announcement Upload]',e);status(e.message||'Falha ao enviar imagem.','error');}finally{uploading=false;$('#announcement')?.classList.remove('cc-ann-uploading');}}
  function install(){style();nav();root();render();load();if(location.hash==='#announcement')setTimeout(()=>openAnnouncement(false),550);const dash=$('#dashboardPanel');if(dash)new MutationObserver(()=>{if(!dash.hidden&&location.hash==='#announcement')requestAnimationFrame(()=>openAnnouncement(false));}).observe(dash,{attributes:true,attributeFilter:['hidden']});window.addEventListener('hashchange',()=>{if(location.hash==='#announcement')setTimeout(()=>openAnnouncement(false),0);});}
  document.addEventListener('input',e=>{if(!e.target.closest('#announcement'))return;const field=e.target.dataset.annField;if(!field)return;doc[field]=e.target.type==='checkbox'?e.target.checked:e.target.value;if(field==='accentColor')$$('[data-ann-field="accentColor"]','#announcement').forEach(x=>{if(x!==e.target)x.value=e.target.value;});updatePreview();status('Alteração ainda não salva.');});
  document.addEventListener('change',e=>{if(!e.target.closest('#announcement'))return;if(e.target.id==='ccAnnImageFile'){upload(e.target.files?.[0]);return;}const field=e.target.dataset.annField;if(field){doc[field]=e.target.type==='checkbox'?e.target.checked:e.target.value;updatePreview();}});
  document.addEventListener('click',e=>{if(!e.target.closest('#announcement'))return;if(e.target.closest('[data-ann-save]'))return save();if(e.target.closest('[data-ann-upload]'))return $('#ccAnnImageFile')?.click();if(e.target.closest('[data-ann-remove-image]')){doc.imageUrl='';const input=$('[data-ann-field="imageUrl"]');if(input)input.value='';updatePreview();status('Imagem removida. Salve para publicar.');}});
  install();
})();