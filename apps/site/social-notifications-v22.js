(() => {
'use strict';
if(window.__CC_NOTIFICATIONS_V22__)return;window.__CC_NOTIFICATIONS_V22__=true;
const db=window.getCosplayChessParticipantDb?window.getCosplayChessParticipantDb():window.COSPLAYCHESS_PARTICIPANT_DB;if(!db)return;
const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)],esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const safe=v=>{try{const u=new URL(String(v||''),location.href);return ['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}};
const fmt=v=>{try{return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(v))}catch{return''}};
let me=null,profiles=new Map(),popover=null,filter='all';
async function getMe(){if(me)return me;const {data:s}=await db.auth.getSession();const uid=s?.session?.user?.id;if(!uid)return null;const {data}=await db.from('cosplay_participant_profiles').select('id').eq('user_id',uid).neq('registration_status','cancelled').order('created_at',{ascending:false}).limit(1).maybeSingle();me=data||null;return me}
async function loadProfiles(ids){const missing=[...new Set(ids.filter(Boolean))].filter(id=>!profiles.has(id));if(!missing.length)return;const {data}=await db.from('cosplay_participant_profiles').select('id,public_slug,display_name,nick,character_name,character_photo_url').in('id',missing);(data||[]).forEach(p=>profiles.set(p.id,p))}
const nameOf=p=>p?.display_name||p?.nick||'CosplayChess';
function avatar(p){const src=safe(p?.character_photo_url);return `<span class="cc22-avatar">${src?`<img src="${esc(src)}" alt="">`:'♜'}</span>`}
function icon(kind){return kind==='message'?'✉':kind==='friend_request'||kind==='friend_accepted'?'♡':kind==='follow'?'＋':kind==='post_like'?'♥':kind==='tag'?'@':kind==='community_join'?'♙':kind==='testimonial'?'✦':'♧'}
async function rows(limit=80){const mine=await getMe();if(!mine)return[];let query=db.from('cosplay_social_notifications').select('id,actor_profile_id,kind,entity_type,entity_id,body,read_at,created_at').eq('recipient_profile_id',mine.id).order('created_at',{ascending:false}).limit(limit);const {data}=await query;const list=data||[];await loadProfiles(list.map(x=>x.actor_profile_id));return list}
function setBadges(count){qa('[data-community-view="notifications"] .social-v2-badge').forEach(b=>{b.textContent=String(count);b.hidden=!count});const b=q('#ccNotificationBadge');if(b){b.textContent=String(count);b.hidden=!count}}
async function refreshBadge(){const mine=await getMe();if(!mine)return;const {count}=await db.from('cosplay_social_notifications').select('id',{count:'exact',head:true}).eq('recipient_profile_id',mine.id).is('read_at',null);setBadges(count||0)}
async function markOne(row){if(!row.read_at){await db.from('cosplay_social_notifications').update({read_at:new Date().toISOString()}).eq('id',row.id);row.read_at=new Date().toISOString();refreshBadge()}}
async function action(row){await markOne(row);const actor=profiles.get(row.actor_profile_id);if(row.entity_type==='message'&&row.entity_id){const {data:m}=await db.from('cosplay_direct_messages').select('sender_profile_id,recipient_profile_id').eq('id',row.entity_id).maybeSingle();const mine=await getMe();const peer=m?(m.sender_profile_id===mine.id?m.recipient_profile_id:m.sender_profile_id):row.actor_profile_id;if(peer){location.href=`./comunidade.html?message=${encodeURIComponent(peer)}`;return}}
if((row.entity_type==='post'||row.kind==='post_like'||row.kind==='tag')&&row.entity_id){location.href=`./comunidade.html?post=${encodeURIComponent(row.entity_id)}`;return}
if(row.entity_type==='community'&&row.entity_id){const {data:c}=await db.from('cosplay_communities').select('slug').eq('id',row.entity_id).maybeSingle();if(c?.slug){location.href=`./comunidade-grupo.html?slug=${encodeURIComponent(c.slug)}`;return}}
if(row.entity_type==='friendship'||row.kind==='friend_request'||row.kind==='friend_accepted'){sessionStorage.setItem('cc-open-social-view','friends');location.href='./comunidade.html';return}
if(actor?.public_slug){location.href=`./jogador.html?slug=${encodeURIComponent(actor.public_slug)}`;return}
location.href='./comunidade.html'}
function ensurePopover(){if(popover)return popover;popover=document.createElement('div');popover.className='cc22-popover';popover.hidden=true;document.body.appendChild(popover);return popover}
function position(){const bell=q('.cc-right-head [data-community-view="notifications"]');if(!bell||!popover)return;const r=bell.getBoundingClientRect();const w=Math.min(390,innerWidth-24);popover.style.top=`${Math.min(innerHeight-80,r.bottom+8)}px`;popover.style.left=`${Math.max(12,Math.min(innerWidth-w-12,r.right-w))}px`}
async function openPreview(){const p=ensurePopover(),list=(await rows(3));p.innerHTML=`<div class="cc22-pop-head"><b>Notificações</b><span>${list.filter(x=>!x.read_at).length?'Novas atividades':'Tudo em dia'}</span></div><div class="cc22-mini-list">${list.length?list.map((row,i)=>{const actor=profiles.get(row.actor_profile_id);return `<button class="cc22-mini ${row.read_at?'':'unread'}" type="button" data-index="${i}">${avatar(actor)}<span class="cc22-copy"><b>${esc(nameOf(actor))}</b><span>${esc(row.body||'Nova atividade na sua rede.')}</span><small>${esc(fmt(row.created_at))}</small></span>${row.read_at?'':'<i class="cc22-unread"></i>'}</button>`}).join(''):'<div class="cc22-empty">Nenhuma notificação ainda.</div>'}</div><button class="cc22-more" type="button">Ver mais notificações</button>`;p.hidden=false;position();qa('[data-index]',p).forEach(b=>b.onclick=()=>action(list[Number(b.dataset.index)]));q('.cc22-more',p).onclick=()=>{p.hidden=true;openFull()}}
async function openFull(){qa('[data-community-panel]').forEach(p=>{const on=p.dataset.communityPanel==='notifications';p.hidden=!on;p.classList.toggle('active',on)});qa('[data-community-view]').forEach(b=>b.classList.toggle('active',b.dataset.communityView==='notifications'));document.body.dataset.ccView='notifications-v22';const panel=q('[data-community-panel="notifications"]');if(!panel)return;panel.innerHTML='<div class="cc22-empty">Carregando notificações...</div>';const list=await rows(100);const filtered=filter==='all'?list:list.filter(x=>filter==='unread'?!x.read_at:x.kind.includes(filter));panel.innerHTML=`<section class="cc22-page"><div class="cc22-page-head"><div><h2>Notificações</h2><p>Acompanhe curtidas, mensagens, amizades, marcações e atividades da sua rede.</p></div><button class="cc22-readall" type="button">Marcar tudo como lido</button></div><section class="cc22-card"><div class="cc22-filter"><button data-filter="all" class="${filter==='all'?'active':''}">Todas</button><button data-filter="unread" class="${filter==='unread'?'active':''}">Não lidas</button><button data-filter="message" class="${filter==='message'?'active':''}">Mensagens</button><button data-filter="friend" class="${filter==='friend'?'active':''}">Amizades</button><button data-filter="post" class="${filter==='post'?'active':''}">Publicações</button></div><div class="cc22-list">${filtered.length?filtered.map((row,i)=>{const actor=profiles.get(row.actor_profile_id);return `<button class="cc22-row ${row.read_at?'':'unread'}" type="button" data-row="${i}">${avatar(actor)}<span class="cc22-copy"><b>${esc(nameOf(actor))}</b><span>${esc(row.body||'Nova atividade na sua rede.')}</span><small>${esc(fmt(row.created_at))}</small></span><span class="cc22-kind">${icon(row.kind)}</span></button>`}).join(''):'<div class="cc22-empty">Nenhuma notificação nesta categoria.</div>'}</div></section></section>`;qa('[data-row]',panel).forEach(b=>b.onclick=()=>action(filtered[Number(b.dataset.row)]));qa('[data-filter]',panel).forEach(b=>b.onclick=()=>{filter=b.dataset.filter;openFull()});q('.cc22-readall',panel).onclick=async()=>{const mine=await getMe();await db.from('cosplay_social_notifications').update({read_at:new Date().toISOString()}).eq('recipient_profile_id',mine.id).is('read_at',null);setBadges(0);openFull()}}
function bootIntent(){const intent=sessionStorage.getItem('cc-open-social-view');if(intent){sessionStorage.removeItem('cc-open-social-view');setTimeout(()=>q(`[data-community-view="${intent}"]`)?.click(),250)}const post=new URLSearchParams(location.search).get('post');if(post){setTimeout(()=>{q('[data-community-view="feed"]')?.click();const banner=document.createElement('div');banner.style.cssText='margin:0 0 10px;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:var(--panel);color:var(--muted);font:700 9px Inter,system-ui';banner.textContent='Abrindo a publicação relacionada à notificação…';q('[data-community-panel="feed"]')?.prepend(banner);setTimeout(()=>banner.remove(),3500)},300)}}
document.addEventListener('click',e=>{const top=e.target.closest('.cc-right-head [data-community-view="notifications"]');if(top){e.preventDefault();e.stopImmediatePropagation();const p=ensurePopover();if(p.hidden)openPreview();else p.hidden=true;return}const left=e.target.closest('.community-nav [data-community-view="notifications"]');if(left){e.preventDefault();e.stopImmediatePropagation();popover&&(popover.hidden=true);openFull();return}if(popover&&!popover.hidden&&!e.target.closest('.cc22-popover'))popover.hidden=true},true);window.addEventListener('resize',()=>{if(popover&&!popover.hidden)position()},{passive:true});refreshBadge();bootIntent();
})();